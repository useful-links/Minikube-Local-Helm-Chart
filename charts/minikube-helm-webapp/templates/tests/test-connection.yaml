apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "minikube-helm-webapp.fullname" . }}-test-connection"
  labels:
{{ include "minikube-helm-webapp.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "minikube-helm-webapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
